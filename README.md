# plasma-power-workflow

KDE Plasma power workflow plasmoid.

Adds a configurable workflow stage before logout, reboot, or shutdown. The plasmoid executes an external workflow, displays progress, and performs the requested action only after successful completion.

Target platform: Plasma 5.
Development platform: Plasma 6.
